import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'comcip',
  webDir: 'dist' // always use mobile build output for Android/iOS
};

export default config;
